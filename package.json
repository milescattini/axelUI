{
  "name": "axel",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:tokens": "style-dictionary build --config design/style-dictionary.config.json",
    "build": "npm run build:tokens",
    "deploy": "npm run build && wrangler pages deploy public",
    "dev": "npm run build:tokens"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0",
    "wrangler": "^3.100.0"
  }
}
